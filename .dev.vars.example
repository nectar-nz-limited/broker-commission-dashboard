# The API Worker uses an encrypted, production-only Integrations Hub identity.
# Never commit real values. Configure these as Worker secrets, not frontend variables.
INTEGRATIONS_HUB_CLIENT_ID=
INTEGRATIONS_HUB_CLIENT_SECRET=
